[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "run-codeql"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
run-codeql = "run_codeql:main"
rcql = "run_codeql:main"
